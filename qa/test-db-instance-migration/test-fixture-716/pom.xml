<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.finture.bpm.qa.upgrade</groupId>
    <artifactId>flowee-bpms-qa-db-instance-migration</artifactId>
    <version>0.5.1</version>
  </parent>

  <artifactId>flowee-bpms-qa-upgrade-test-fixture-716</artifactId>
  <packaging>jar</packaging>
  <name>flowee-bpms BPM - QA - upgrade - instance migration - test fixture - 7.16.0</name>

  <properties>
    <flowee-bpms.version.previous>7.15.0</flowee-bpms.version.previous>
    
    <version.h2>${version.h2-v1}</version.h2>
  </properties>

  <!-- uncomment when 7.16 is released -->
   <dependencyManagement>
     <dependencies>
       <dependency>
         <groupId>com.finture.bpm</groupId>
         <artifactId>flowee-bpms-bom</artifactId>
         <version>7.16.0</version>
         <scope>import</scope>
         <type>pom</type>
       </dependency>
     </dependencies>
   </dependencyManagement>


  <dependencies>
    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>com.finture.spin</groupId>
      <artifactId>flowee-bpms-spin-dataformat-json-jackson</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine-plugin-spin</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm.qa.upgrade</groupId>
      <artifactId>flowee-bpms-qa-upgrade-scenario-util</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>true</filtering>
      </testResource>
    </testResources>
  </build>

  <profiles>
    <profile>
      <id>instance-migration</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <id>create-sql-script-stubs</id>
                <phase>generate-test-sources</phase>
                <configuration>
                  <target>
                    <property name="current.minor.version" value="${flowee-bpms.current.majorVersion}.${flowee-bpms.current.minorVersion}" />
                    <property name="previous.minor.version" value="${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}" />
                    <property name="current.sql.upgrade.dir" value="${project.build.directory}/scripts-current/sql/upgrade" />
                    <property name="minor.version.upgrade.script.previous.to.current.engine" value="${database.type}_engine_${previous.minor.version}_to_${current.minor.version}.sql" />
                    <property name="minor.version.upgrade.script.previous.to.current.identity" value="${database.type}_identity_${previous.minor.version}_to_${current.minor.version}.sql" />
                    <property name="patch.version.upgrade.script.previous.engine.prefix" value="${database.type}_engine_${previous.minor.version}_patch_${previous.minor.version}" />

                    <!-- create the SQL scripts so that the files exist even if they do not exist in the distribution. (this can be the case if there are no db
                      upgrades (yet) for a particular release ) -->

                    <mkdir dir="${current.sql.upgrade.dir}/" />

                    <!-- concatenated patches for last minor version -->
                    <!-- 7.16.0 is on a clean state so we can again use the concatenated patches -->
                    <touch file="${project.build.directory}/scripts-current/sql/upgrade/${database.type}_engine_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_patch.sql" />
                    <touch file="${project.build.directory}/scripts-current/sql/upgrade/${database.type}_identity_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_patch.sql" />

                    <!-- minor version upgrade from previous to current version -->
                    <touch file="${current.sql.upgrade.dir}/${minor.version.upgrade.script.previous.to.current.engine}" />
                    <touch file="${current.sql.upgrade.dir}/${minor.version.upgrade.script.previous.to.current.identity}" />
                  </target>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>

          <!-- provide sql scripts -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack-current-sql-scripts</id>
                <phase>generate-test-sources</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>com.finture.bpm.distro</groupId>
                      <artifactId>flowee-bpms-sql-scripts</artifactId>
                      <version>7.16.0</version>
                      <type>test-jar</type>
                      <outputDirectory>${project.build.directory}/scripts-current</outputDirectory>
                      <overWrite>true</overWrite>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- parse version properties from qa/pom.xml -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>parse-current-version</id>
                <goals>
                  <goal>parse-version</goal>
                </goals>
                <configuration>
                  <propertyPrefix>flowee-bpms.current</propertyPrefix>
                  <versionString>7.16.0</versionString>
                </configuration>
              </execution>
              <execution>
                <id>parse-old-version</id>
                <goals>
                  <goal>parse-version</goal>
                </goals>
                <configuration>
                  <propertyPrefix>flowee-bpms.previous</propertyPrefix>
                  <versionString>${flowee-bpms.version.previous}</versionString>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>sql-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>patch-previous-schema</id>
                <phase>test-compile</phase>
                <goals>
                  <goal>execute</goal>
                </goals>
                <configuration>
                  <srcFiles>
                    <!-- now patch old version of the schema -->
                    <!--<srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_engine_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_patch_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}.x_to_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}.y.sql</srcFile>-->
                    <srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_engine_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_patch.sql</srcFile>
                    <srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_identity_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_patch.sql</srcFile>
                  </srcFiles>
                </configuration>
              </execution>
              <execution>
                <id>upgrade-db</id>
                <phase>test-compile</phase>
                <goals>
                  <goal>execute</goal>
                </goals>
                <configuration>
                  <srcFiles>
                    <!-- now perform the schema migrations -->
                    <srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_engine_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_to_${flowee-bpms.current.majorVersion}.${flowee-bpms.current.minorVersion}.sql</srcFile>
                    <srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_identity_${flowee-bpms.previous.majorVersion}.${flowee-bpms.previous.minorVersion}_to_${flowee-bpms.current.majorVersion}.${flowee-bpms.current.minorVersion}.sql</srcFile>
                  </srcFiles>
                </configuration>
              </execution>
              <!-- example: patch-current-schema
              <execution>
                <id>patch-current-schema</id>
                <phase>test-compile</phase>
                <goals>
                  <goal>execute</goal>
                </goals>
                <configuration>
                  <srcFiles>
                    // now patch current version of the schema
                    // example for aggregate patching of current version
                    <srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_engine_${flowee-bpms.current.majorVersion}.${flowee-bpms.current.minorVersion}_patch.sql</srcFile>
                    <srcFile>${project.build.directory}/scripts-current/sql/upgrade/${database.type}_identity_${flowee-bpms.current.majorVersion}.${flowee-bpms.current.minorVersion}_patch.sql</srcFile>
                  </srcFiles>
                </configuration>
              </execution>
              -->
            </executions>
          </plugin>
          
          <!-- Initialize Liquibase so that we can make updates to higher 
            versions in following test fixtures -->
          <plugin>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>initialize-liquibase</id>
                <phase>test-compile</phase>
                <goals>
                  <goal>changelogSyncToTag</goal>
                </goals>
                <configuration>
                  <toTag>7.16.0</toTag>
                  <changeLogFile>flowee-bpms-changelog.xml</changeLogFile>
                  <changeLogDirectory>${project.build.directory}/scripts-current/sql/liquibase/</changeLogDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>
          
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <configuration>
              <cleanupDaemonThreads>false</cleanupDaemonThreads>
              <includeProjectDependencies>true</includeProjectDependencies>
              <includePluginDependencies>false</includePluginDependencies>
              <classpathScope>test</classpathScope>
            </configuration>
            <executions>
              <execution>
                <id>create-test-fixture</id>
                <goals>
                  <goal>java</goal>
                </goals>
                <phase>process-test-classes</phase>
                <configuration>
                  <mainClass>com.finture.bpm.qa.upgrade.TestFixture</mainClass>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    
    <profile>
      <id>h2</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>sql-maven-plugin</artifactId>
            <executions>
              <!-- In order to migrate from H2 1.4 to 2.x, we
                must make a backup of our data and import it 
                with the new version (see 717 fixture) -->
              <execution>
                <id>export-database-h2-14</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>execute</goal>
                </goals>
                <configuration>
                  <sqlCommand>SCRIPT TO '${database.host}changelog.h2.sql' CHARSET 'UTF-8'</sqlCommand>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
