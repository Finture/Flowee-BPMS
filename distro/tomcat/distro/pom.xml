<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <artifactId>flowee-bpms-bpm-tomcat</artifactId>
  <packaging>pom</packaging>

  <parent>
    <groupId>com.finture.bpm.tomcat</groupId>
    <artifactId>flowee-bpms-tomcat</artifactId>
    <version>0.7.1-SNAPSHOT</version>
  </parent>

  <name>Flowee BPMS Platform - tomcat Distro</name>

  <dependencies>

    <dependency>
      <groupId>com.finture.bpm.distro</groupId>
      <artifactId>flowee-bpms-sql-scripts</artifactId>
      <version>${project.version}</version>
      <type>jar</type>
    </dependency>

    <!-- Tomcat 10 dependencies -->

    <dependency>
      <groupId>com.finture.bpm.tomcat</groupId>
      <artifactId>flowee-bpms-tomcat-assembly</artifactId>
      <version>${project.version}</version>
      <type>tar.gz</type>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm.webapp</groupId>
      <artifactId>flowee-bpms-webapp-tomcat-jakarta</artifactId>
      <version>${project.version}</version>
      <type>war</type>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine-rest-jakarta</artifactId>
      <version>${project.version}</version>
      <classifier>tomcat</classifier>
      <type>war</type>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm.example</groupId>
      <artifactId>flowee-bpms-example-invoice-jakarta</artifactId>
      <version>${project.version}</version>
      <type>war</type>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>distro-tomcat10</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>assembly.xml</descriptor>
              </descriptors>
              <finalName>flowee-bpms-bpm-tomcat-${project.version}</finalName>
              <attach>true</attach>
              <appendAssemblyId>false</appendAssemblyId>
              <outputDirectory>target/</outputDirectory>
              <workDirectory>target/assembly/work</workDirectory>
              <tarLongFileMode>gnu</tarLongFileMode>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
