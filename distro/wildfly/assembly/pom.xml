<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.finture.bpm.wildfly</groupId>
    <artifactId>flowee-bpms-wildfly</artifactId>
    <version>0.7.1</version>
  </parent>

  <artifactId>flowee-bpms-wildfly-assembly</artifactId>
  <packaging>pom</packaging>

  <name>Flowee BPMS Platform - Wildfly Assembly</name>

  <description>
    Assembles the Flowee BPMS Platform and makes it ready for qa.
    The assembly is installed to maven later in the reactor, if the integration tests pass successfully
  </description>
  
  <properties>
    <!-- generate a bom of compile time dependencies for the license book.
    Note: Every compile time dependency will end up in the license book. Please
    declare only dependencies that are actually needed -->
    <skip-third-party-bom>false</skip-third-party-bom>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.wildfly</groupId>
      <artifactId>wildfly-dist</artifactId>
      <version>${version.wildfly}</version>
      <type>tar.gz</type>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm.javaee</groupId>
      <artifactId>flowee-bpms-ejb-client-jakarta</artifactId>
    </dependency>
    
    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine</artifactId>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm.identity</groupId>
      <artifactId>flowee-bpms-identity-ldap</artifactId>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine-cdi-jakarta</artifactId>
    </dependency>
    
    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine-spring-6</artifactId>
    </dependency>
    
    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>flowee-bpms-engine-rest-jakarta</artifactId>
      <classifier>classes</classifier>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>com.finture.bpm</groupId>
      <artifactId>license-book</artifactId>
      <version>${project.version}</version>
      <type>zip</type>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.uuid</groupId>
      <artifactId>java-uuid-generator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-jsr223</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-json</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-xml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-templates</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-dateutil</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.groovy</groupId>
      <artifactId>groovy-datetime</artifactId>
    </dependency>

    <dependency>
      <groupId>org.graalvm.js</groupId>
      <artifactId>js</artifactId>
    </dependency>
    <dependency>
      <groupId>org.graalvm.js</groupId>
      <artifactId>js-scriptengine</artifactId>
    </dependency>

    <dependency>
      <!-- this dependency is to make sure that we are executed after camunda-modules 
        in the reactor -->
      <groupId>com.finture.bpm.wildfly</groupId>
      <artifactId>flowee-bpms-wildfly-modules</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>assemble</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>assembly.xml</descriptor>
              </descriptors>
              <attach>true</attach>
              <appendAssemblyId>false</appendAssemblyId>
              <outputDirectory>target/</outputDirectory>
              <workDirectory>target/assembly/work</workDirectory>
              <tarLongFileMode>gnu</tarLongFileMode>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
